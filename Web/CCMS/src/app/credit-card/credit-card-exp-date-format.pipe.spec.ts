import { CreditCardExpDateFormatPipe } from './credit-card-exp-date-format.pipe';

describe('CreditCardExpDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardExpDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
